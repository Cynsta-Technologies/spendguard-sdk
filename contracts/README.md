# API Contracts

- `openapi.json` is generated from active wrapper routes (`spendguard-sidecar`; include `spendguard-cloud` routes if they are public and part of the SDK contract).
- Any wire-format change must update this file in the same PR.
